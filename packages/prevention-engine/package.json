{
  "name": "@holi/prevention-engine",
  "version": "0.1.0",
  "description": "Health 3.0 — Rule-based prevention alert evaluator with ELENA invariant compliance",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@holi/data-ingestion": "workspace:*",
    "@holi/event-bus": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.3",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
