/**
 * Traffic Light Components
 *
 * UI components for displaying and interacting with the Traffic Light
 * rules evaluation system.
 *
 * @module components/traffic-light
 */

export { TrafficLight, TrafficLightIndicator } from './TrafficLight';
export type {
  TrafficLightColor,
  TrafficLightResult,
  TrafficLightSignal,
} from '@/lib/traffic-light/types';
