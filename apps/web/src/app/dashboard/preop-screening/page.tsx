/**
 * Dashboard-integrated pre-op supplement screening.
 *
 * Used by clinicians to build a stop schedule for a patient during a pre-op
 * visit. The underlying component is shared with the public patient-facing
 * page at /find-doctor/preop-screening — here we render it inside the
 * authenticated dashboard chrome.
 */
export { default } from '../../find-doctor/preop-screening/page';
