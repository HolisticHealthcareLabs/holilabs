/**
 * Dashboard-integrated provider directory.
 *
 * Used by clinicians making cross-modality referrals from within the Holi Labs
 * product. Same directory as the public /find-doctor; this surfaces it inside
 * the authenticated dashboard chrome (sidebar, branding, auth).
 */
export { default } from '../../find-doctor/page';
