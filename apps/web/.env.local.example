# Database
DATABASE_URL="postgresql://user:password@localhost:5432/holi_labs?schema=public"

# Encryption (CRITICAL - HIPAA Required)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY="your-32-byte-hex-encryption-key-here"

# AI Services (Required for Core Functionality)

# AssemblyAI - Audio Transcription
# Cost: ~$0.015/minute (50% cheaper than Whisper)
# Features: Portuguese/Spanish support, speaker diarization, PHI redaction
# Sign up: https://www.assemblyai.com/
ASSEMBLYAI_API_KEY="your-assemblyai-api-key"

# Google AI (Gemini 2.0 Flash) - SOAP Note Generation
# Cost: ~$0.003/note (35x cheaper than Claude)
# Features: Medical reasoning, multilingual, fast (250 tok/sec)
# Sign up: https://ai.google.dev/
GOOGLE_AI_API_KEY="your-google-ai-api-key"

# Supabase (Required for file storage)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# Next.js (Required)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret"

# Optional: Logging
LOGTAIL_SOURCE_TOKEN="your-logtail-token"

# Optional: Redis (for distributed rate limiting)
UPSTASH_REDIS_REST_URL="https://your-redis.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-redis-token"

# Optional: Connection pool tuning
DB_POOL_SIZE=10
DB_TIMEOUT=10000
DB_QUERY_TIMEOUT=15000
DB_POOL_TIMEOUT=10000

# ============================================================================
# COST ANALYSIS (per doctor, 20 patients/day, 200 days/year)
# ============================================================================
# AssemblyAI Transcription: $120/year ($10/month)
# Gemini 2.0 Flash SOAP:     $3.20/year ($0.27/month)
# ───────────────────────────────────────────────────────
# TOTAL AI COST:            $123.20/year ($10.25/month)
#
# Compared to previous stack:
# - Whisper + Claude:       $234/year ($19.50/month)
# - Savings:                $111/year (47% cheaper)
#
# ============================================================================
# LANGUAGE SUPPORT
# ============================================================================
# Portuguese: ✅ Full support (Brazil)
# Spanish:    ✅ Full support (Mexico, Colombia, Argentina, etc.)
#
# Features:
# - Automatic language detection based on patient country
# - Medical terminology in both languages
# - Speaker diarization (Doctor vs Patient)
# - PHI redaction (HIPAA compliance)
# ============================================================================
